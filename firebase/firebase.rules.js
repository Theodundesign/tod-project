/**
 * Combined rules overview (informational). Deploy the separate `firestore.rules` and `storage.rules` files.
 * - firestore.rules
 * - storage.rules
 *
 * This file is a reference and not used by the Firebase CLI.
 */

module.exports = {
  firestore: 'firebase/firestore.rules',
  storage: 'firebase/storage.rules'
}
